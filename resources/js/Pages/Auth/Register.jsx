import { Link, useForm } from '@inertiajs/react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faArrowRight, faCheck } from '@fortawesome/free-solid-svg-icons';
import { Brand } from '../../Components/AppShell';

export default function Register() { const form = useForm({ name: '', email: '', password: '', password_confirmation: '' }); return <div className="auth-page"><Link href="/" className="brand" aria-label="BISA beranda"><Brand /></Link><div className="auth-wrap"><div className="auth-copy"><span className="kicker">Mulai dari sini</span><h1>Satu langkah<br /><em>lebih dekat.</em></h1><p>Bangun skill yang relevan, satu project pada satu waktu.</p><div className="auth-perks"><span><FontAwesomeIcon icon={faCheck} /> Gratis selamanya</span><span><FontAwesomeIcon icon={faCheck} /> Roadmap terarah</span><span><FontAwesomeIcon icon={faCheck} /> Bukti untuk portfolio</span></div></div><form className="auth-card" onSubmit={(event) => { event.preventDefault(); form.post('/register'); }}><h2>Buat akun BISA</h2><p className="auth-muted">Tidak perlu kartu kredit.</p><label>Nama lengkap<input value={form.data.name} onChange={(e) => form.setData('name', e.target.value)} placeholder="Nama kamu" /></label><label>Email<input type="email" value={form.data.email} onChange={(e) => form.setData('email', e.target.value)} placeholder="nama@email.com" /></label><label>Password<input type="password" value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} placeholder="Minimal 8 karakter" /></label><label>Ulangi password<input type="password" value={form.data.password_confirmation} onChange={(e) => form.setData('password_confirmation', e.target.value)} placeholder="Ketik ulang password" /></label><button className="button button-dark auth-submit" disabled={form.processing}>Buat akun <FontAwesomeIcon icon={faArrowRight} /></button><p className="auth-footer">Sudah punya akun? <Link href="/login">Masuk</Link></p></form></div></div>; }