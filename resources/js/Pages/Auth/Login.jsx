import { Link, useForm } from '@inertiajs/react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faArrowRight, faLock } from '@fortawesome/free-solid-svg-icons';
import { Brand } from '../../Components/AppShell';

export default function Login() { const form = useForm({ email: '', password: '', remember: false }); return <div className="auth-page"><Link href="/" className="brand" aria-label="BISA beranda"><Brand /></Link><div className="auth-wrap"><div className="auth-copy"><span className="kicker">Selamat datang kembali</span><h1>Teruskan<br /><em>langkahmu.</em></h1><p>Skill yang kamu bangun hari ini akan membuka lebih banyak pilihan esok hari.</p></div><form className="auth-card" onSubmit={(event) => { event.preventDefault(); form.post('/login'); }}><h2>Masuk ke BISA</h2><p className="auth-muted">Lanjutkan perjalanan belajarmu.</p><label>Email<input type="email" value={form.data.email} onChange={(e) => form.setData('email', e.target.value)} placeholder="nama@email.com" />{form.errors.email && <small className="form-error" role="alert">{form.errors.email}</small>}</label><label>Password<div className="input-icon"><input type="password" value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} placeholder="••••••••" /><FontAwesomeIcon icon={faLock} /></div></label><label className="check-label"><input type="checkbox" checked={form.data.remember} onChange={(e) => form.setData('remember', e.target.checked)} /> Ingat saya</label><button className="button button-dark auth-submit" disabled={form.processing}>Masuk <FontAwesomeIcon icon={faArrowRight} /></button><p className="auth-footer">Belum punya akun? <Link href="/register">Daftar gratis</Link></p></form></div></div>; }