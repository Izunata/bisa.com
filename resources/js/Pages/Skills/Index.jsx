import { Link } from '@inertiajs/react';
import { motion } from 'framer-motion';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faArrowRight, faClock, faMagnifyingGlass, faUsers } from '@fortawesome/free-solid-svg-icons';
import AppShell from '../../Components/AppShell';

const colors = ['lime', 'blue', 'orange'];
export default function Index({ skills = [], categories = [] }) { return <AppShell><div className="explore-head"><div><span className="kicker">Explore skill</span><h1>Temukan arah<br /><em>yang terasa milikmu.</em></h1><p>Skill praktis, roadmap jelas, dan project nyata untuk membawamu maju.</p></div><div className="search-box"><FontAwesomeIcon icon={faMagnifyingGlass} /><input placeholder="Cari skill atau topik..." /></div></div><div className="filter-row"><button className="filter-pill active">Semua skill</button>{categories.map((category) => <button className="filter-pill" key={category}>{category}</button>)}</div><div className="skill-catalog">{skills.map((skill, index) => <motion.div key={skill.id} initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .06 }}><Link href={`/skills/${skill.slug}`} className="catalog-card"><div className={`catalog-art ${colors[index % colors.length]}`}><span>{['✦', '↗', '◎'][index % 3]}</span><small>{skill.level}</small></div><div className="catalog-body"><span className="tile-tag">{skill.category}</span><h2>{skill.name}</h2><p>{skill.description}</p><div className="catalog-meta"><span><FontAwesomeIcon icon={faClock} /> {skill.estimated_hours} jam</span><span><FontAwesomeIcon icon={faUsers} /> {skill.learners_count.toLocaleString('id-ID')}</span><span className="catalog-arrow"><FontAwesomeIcon icon={faArrowRight} /></span></div></div></Link></motion.div>)}</div>{skills.length === 0 && <div className="empty-catalog"><h2>Skill sedang disiapkan.</h2><p>Jadilah yang pertama tahu saat jalur belajar baru dibuka.</p></div>}</AppShell>; }