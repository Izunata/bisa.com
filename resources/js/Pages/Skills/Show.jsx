import { Link, router } from '@inertiajs/react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faArrowLeft, faArrowRight, faCheck, faClock, faLock, faPlay, faRocket } from '@fortawesome/free-solid-svg-icons';
import AppShell from '../../Components/AppShell';

export default function Show({ skill }) { return <AppShell><Link href="/skills" className="back-link"><FontAwesomeIcon icon={faArrowLeft} /> Semua skill</Link><div className="skill-detail-head"><div><span className="tile-tag">{skill.category} · {skill.level}</span><h1>{skill.name}</h1><p>{skill.description}</p><div className="detail-meta"><span><FontAwesomeIcon icon={faClock} /> {skill.estimated_hours} jam belajar</span><span>•</span><span>Roadmap terstruktur</span></div><button className="button button-dark" onClick={() => router.post(`/skills/${skill.slug}/enroll`)}>Mulai belajar <FontAwesomeIcon icon={faArrowRight} /></button></div><div className="detail-art"><span>{skill.name.charAt(0)}</span><div className="art-stamp">BISA<br />SKILL</div></div></div><div className="roadmap-layout"><section><div className="panel-heading"><div><span className="kicker">The roadmap</span><h2>Langkah belajarmu</h2></div><span className="roadmap-total">0% dimulai</span></div><div className="roadmap-line">{skill.modules?.map((module, index) => <div className="roadmap-module" key={module.id}><div className={`roadmap-node ${index === 0 ? 'current' : ''}`}>{index === 0 ? <FontAwesomeIcon icon={faPlay} /> : <FontAwesomeIcon icon={faLock} />}</div><div className="module-content"><span className="module-number">0{index + 1} · {index === 0 ? 'MULAI DI SINI' : 'SELANJUTNYA'}</span><h3>{module.title}</h3><p>{module.description}</p>{module.lessons?.map((lesson) => <div className="lesson-row" key={lesson.id}><span className="lesson-icon"><FontAwesomeIcon icon={lesson.type === 'practice' ? faRocket : faPlay} /></span><span>{lesson.title}<small>{lesson.duration_minutes} menit · {lesson.type}</small></span><FontAwesomeIcon icon={index === 0 ? faArrowRight : faLock} className="lesson-action" /></div>)}</div></div>)}</div></section><aside className="roadmap-side"><div className="side-quote"><span>✦</span><p>“Progress kecil yang konsisten selalu menang dari niat besar yang ditunda.”</p></div><div className="project-card"><span className="kicker">Bukti akhir</span><div className="project-icon"><FontAwesomeIcon icon={faRocket} /></div><h3>Project nyata menunggumu</h3><p>Bangun sesuatu yang bisa kamu ceritakan di portfolio.</p><ul><li><FontAwesomeIcon icon={faCheck} /> Brief yang jelas</li><li><FontAwesomeIcon icon={faCheck} /> Feedback terarah</li><li><FontAwesomeIcon icon={faCheck} /> Siap dipamerkan</li></ul></div></aside></div></AppShell>; }